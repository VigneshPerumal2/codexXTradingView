#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

export TV_CDP_PORT="${TV_CDP_PORT:-9333}"

exec "$ROOT_DIR/scripts/tv-cli" launch -p "$TV_CDP_PORT" "$@"
