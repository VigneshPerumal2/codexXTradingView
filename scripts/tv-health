#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

export TV_CDP_PORT="${TV_CDP_PORT:-9333}"

"$ROOT_DIR/scripts/tv-cli" status
echo
"$ROOT_DIR/scripts/tv-cli" state
